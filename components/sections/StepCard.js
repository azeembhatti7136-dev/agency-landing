"use client";

import { motion } from "framer-motion";

export default function StepCard({ step, colors, index, totalSteps }) {
  return (
    <div className="step-card">
      {/* Step card implementation */}
    </div>
  );
}